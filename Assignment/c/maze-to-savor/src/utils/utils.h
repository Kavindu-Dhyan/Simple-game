#ifndef UTILS_H
#define UTILS_H

// Function declarations for utility functions

// Generates a random number between min and max (inclusive)
int randomNumber(int min, int max);

// Handles user input and returns it as a string
char* getUserInput();

// Clears the console screen
void clearScreen();

// Pauses the program and waits for user input
void pauseProgram();

#endif // UTILS_H